package com.ict05.collection;

public class Ex06 {
	// List 인터페이스를 구현한 클래스들 : Stack, ArrayList, Vector;
	// Array 와 Vector : 배열과 흡사한 구조이지만, 삽입, 삭제, 추가가 자유롭다.
	//					 크기를 미리 지정하지 않아도 된다.
	//					 ArrayList(동기화 지원 X)    Vector(동기화 지원 O)
	// Set 구조는 순서가 없다. 중복 안됨
	// List 구조는 순서가 존재, 중복가능
}
